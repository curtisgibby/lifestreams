const width=1e3,height=600,svg=d3.select("#svg1").attr("width",width).attr("height",height).style("background-color","#eee"),timeScale=d3.scaleTime().domain([new Date(1981,4,3),new Date]).range([0,width]),streamHeight=50;for(const e of streams){let t=e.index*(streamHeight+5);for(const a of e.events)a.endDate||(a.endDate=(new Date).toLocaleDateString()),svg.append("line").style("stroke",a.color||e.color).style("stroke-width",streamHeight).attr("x1",timeScale(Date.parse(a.startDate))).attr("y1",t).attr("x2",timeScale(Date.parse(a.endDate))).attr("y2",t)}